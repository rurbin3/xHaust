const url = require('url')
const colorize = require('json-colorizer')
const chalk = require('chalk')

module.exports = class Request extends require('../classes/package') {
	constructor() {
		super()
	}
}
